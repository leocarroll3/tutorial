---
import Header from "../components/Header.astro";
const { frontmatter } = Astro.props;
---

<Header />
<h1>{frontmatter.title}</h1>
<img src={frontmatter.image.url} width="500" alt={frontmatter.image.alt} />
<p>Text & Graphics by Leo Carroll</p>
<slot />
